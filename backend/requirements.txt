# IST-RVT Backend Requirements
# GPU optional — all models work on CPU with int8 quantization
# Compatible with Python 3.13+

# === Web Framework ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
aiofiles>=23.2.1

# === Auth & Security ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=42.0.5

# === Database ===
sqlalchemy>=2.0.29
aiosqlite>=0.20.0
alembic>=1.13.1

# === AI — Speech-to-Text ===
# faster-whisper: 4x faster than OpenAI Whisper, works on CPU (int8)
faster-whisper>=1.0.1

# === AI — Translation ===
# deep-translator: API-based fallback (Google Translate free tier)
deep-translator>=1.11.4

# === AI — Text-to-Speech ===
# edge-tts: Microsoft Edge TTS, FREE, no API key, 300+ voices, high quality
edge-tts>=6.1.10
# gTTS: Google TTS fallback
gTTS>=2.5.1

# === AI — Voice Cloning (optional, GPU recommended) ===
# TTS includes XTTS-v2 for voice cloning; comment out if having install issues
# TTS==0.22.0

# === Audio Processing ===
numpy>=1.26.4
scipy>=1.13.0
soundfile>=0.12.1
pydub>=0.25.1
librosa>=0.10.2

# === Utilities ===
python-dotenv>=1.0.1
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
redis>=5.0.4
Pillow>=10.3.0
